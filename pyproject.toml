[project]
name = "news-monitor"
version = "0.1.0"
description = "Event-driven news monitoring system"
authors = [{ name = "Developer", email = "developer@example.com" }]
dependencies = [
    "faststream[rabbit,http]",
    "pydantic-settings",
    "feedparser",
    "spacy>=3.7.0",
    "psycopg2-binary",
    "httpx",
    "ruff",
    "sqlalchemy>=2.0.0",
]
